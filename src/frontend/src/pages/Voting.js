import React from 'react';

const Voting = () => {
    return (
        <div>
            <h1>Voting</h1>
            <p>Voting interface will be implemented here.</p>
            {/* Add logic to fetch proposals and allow voting */}
        </div>
    );
};

export default Voting;
